# dotfiles

```sh
cd ~/
clone git@github.com:mokoaki/.dotfiles.git
cd .dotfiles
ruby deploy.rb
```

## fish

```sh
brew install fish
sudo vi /etc/shells
# /usr/local/bin/fish
chsh -s /usr/local/bin/fish
```
